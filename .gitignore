# Environment variables
.env
.credentials.json

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.worktrees/
.claude/worktrees/
.ralf/
.crit.json
.superpowers/
.grind

# Local settings
settings.local.json

# OS specific
.DS_Store

# Windows Zone.Identifier files (created when copying from Windows to WSL)
*:Zone.Identifier

# DevContainer environment variables (contains secrets)
.devcontainer/.env

# MCP state
.playwright-mcp/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

__pycache__/

# Python tooling caches
.coverage*
htmlcov/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/

# optimize-my-skill --deep workspaces (persistent but not committed)
.eval-runs/

# vizsuite artifact output (`viz pr` writes .viz/out/*.html at cwd)
.viz/

# backlog-landscape regenerated artifacts (scripts/backlog-landscape/regenerate.sh)
scripts/backlog-landscape/output/
