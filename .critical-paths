# .critical-paths — anticipates future work. Nothing reads this file.
#
# The selectors below would force the HEAVY tier of a completion gate. Uncomment
# them in the change that deploys a tier router; thresholds go in
# project-config.toml's [completion-gate], commented out for the same reason.
# gitignore syntax, anchored to this repo root.
# src/**
# packages/**
# scripts/**
# .github/**
# Makefile
