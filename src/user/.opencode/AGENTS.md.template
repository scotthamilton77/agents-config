<!-- DYNAMIC-INCLUDE: src/user/.agents/USER-CORE.md.template -->

If `~/.config/opencode/AGENTS.local.md` exists, read it in full first: it holds the user's own always-on instructions, which apply on top of everything above.
