# Handoff — prgroom reviewer-registry seeding (PR #348)

**Written:** 2026-07-19T22:14Z · **Session:** 35acdf0d-09f4-4dd2-904c-98d23fc12041
**Bead:** agents-config-abn9.8.51 (in_progress, claimed) · **PR:** scotthamilton77/agents-config#348
**Branch:** `worktree-prgroom-reviewer-registry-seed` · **Worktree:** `.claude/worktrees/prgroom-reviewer-registry-seed`
**Head (pushed):** `957c5651238c8cbb901c5264c905fe53b34f989c`

## Where things stand

The implementation plan (`docs/plans/2026-07-19-prgroom-reviewer-registry-seed.md`, spec `docs/specs/2026-07-19-prgroom-reviewer-registry-seed-design.md`) is fully delivered: 10 plan tasks plus **26 review-fix commits across 11 Codex review rounds** (PR-side rounds 1–11) and one local Codex plugin cycle (standard + adversarial). Every finding was verified against the code before classification; every fix was TDD'd by a dedicated opus subagent and audited (exactly-one-commit, SHA match).

**Gate at head:** `make ci-prgroom` fully green — 1120 tests, 99.47 % coverage, mypy --strict, ruff, ruff format, pip-audit, entry-verify.

**PR state:** all review threads replied and resolved through round 10. Merge-judge provenance sidecar is current for head `957c565` (all commits anthropic/first-hand).

## Immediate next steps (in order)

1. **Round-11 threads (2) need replies + resolution.** Comments `3611368773` (fixed in `957c565`) and `3611368775` (fixed in `0801fc4`) are unreplied/unresolved. Build a head-`957c565` inventory (pattern: `~/.claude/state/pr-inventory/scotthamilton77-agents-config-348-<full-head-sha>.json` — copy the shape of the `a5fccb4e...` one, which also shows the `duplicate_of`-as-permalink gotcha), include a SKIP for review summary `4731490693`, then render → post (skip-set for the summary) → resolve via the `reply-and-resolve-pr-threads` helpers.
2. **One more Codex verification round** on head `957c565`: `request-rereview.sh` with the cumulative disposition table at `/tmp/pr348-disposition.json` (23 rows; add 2 rows for `0801fc4` and `957c565` before posting), `--since-sha 8c80ac1`. Skip the 80s start probe; background `poll-copilot-review.sh` with `--since-timestamp` captured before the ask and `--timeout-seconds 1200`. **Known false negative:** the poll can time out even though Codex reviewed (~4 min after the ask) — on exit 1, always check `gh api .../reviews` for a review with `submitted_at > ASK_TS` before believing the timeout.
3. **On a clean round:** merge-guard (Steps 1–5). Policy is `rule-based` / `bot-quiescence` (cached policy JSON at `/tmp/pr348-policy.json`; re-resolve from base per the skill if stale). Needs `facts.bot_clean_review_at_head == true` at the current head. Approver App is configured (`MERGE_GUARD_APPROVER_KEY_PATH`). Scott's standing grant (2026-07-19, in memory): merge autonomously iff merge-guard fully clears.
4. **On new findings instead:** verify each against the code first (11 rounds, zero false positives so far — but also zero re-raises thanks to the disposition tables; keep using them). Then report classification to Scott before fixing — the round cap is long spent and he has been ruling per round.
5. **Post-merge:** `sync-after-remote-merge` (ExitWorktree `discard_changes: true` + the handed-back `--finish`), close bead `agents-config-abn9.8.51`, confirm parent epic `abn9.8` state, close `agents-config-abn9.8.55` (format drift — fixed by plan Task 10 commit `b20222e`).

## Review-round ledger (finding → fix commit)

Rounds 1–6 (pre-compaction, threads resolved): `d5bb654`, `3bdcb32`, `6b0f961`, `978f83a`, `393e22d`, `bb92bde`, `da7739f`, `8ed69bf`, `38d704e`, `553b790`, `0700a1d`.
Round 7: `8c80ac1` (equal-second candidate rank), `d94c634` (same-poll seed, later refined), `5193549` (rereview_pr stamp clear).
Local plugin cycle: `968b0a8` (cross-poll equal-ts id), `737e493` (promotion-edge window restart), `ddca392` (defer-one-poll seed disambiguation), `d51b49b` (boundary-coherent id tiebreak).
Rounds 8–9 (auto-reviews on interim pushes): `3b68c58` (invalidation boundary), `e685e03` (NOT_REQUESTED comment gate), `856c232` (QUIESCED push → FIXES_PENDING).
Round 10: `a5fccb4` (optional declined drive-by promoted on first formal request — two duplicate threads).
Round 11: `0801fc4` (self-induced same-second equality in decline-pass suppression), `957c565` (invalidation flip moved ahead of reconciliation; option (a) — state schema has no push timestamp, so status-broadening was unsound).

## Beads filed this session

- `agents-config-abn9.8.56` (P2, out-of-scope): fail-closed `requested_teams` handling — spec behavior 10 scope-out; adversarial reviews re-raise it every pass; SKIP with that rationale.
- `agents-config-abn9.8.57` (P2, in-scope deferred, own-cycle): timeline-events API disambiguation for same-second review-request collisions — the irreducible-precision residual; SKIP rationale for any re-raise.

## Load-bearing invariants (violate any of these and Codex will find it)

- A login leaves `requested_reviewers` the instant it submits ANY review → co-presence of request + review proves the request is newer (except within one GitHub second — the `abn9.8.57` residual).
- A timeout-declined **required** reviewer stays continuously present in `requested_reviewers` (declines are in-memory only) → bare presence never restarts its window (`bb92bde`); but an **optional** declined drive-by was never requested, so its presence IS a first formal request (`a5fccb4`).
- Coherence invariant: `last_review_id` is the id of the review observed at `last_review_at`, else None; the reactivation tiebreak applies only when the boundary IS `last_review_at` (`d51b49b`).
- External push invalidation must precede reconciliation and stamps `last_request_at = now` as the boundary (`957c565`, `3b68c58`); NOT_REQUESTED leaves only via re-request or post-boundary terminal verdict (`e685e03`).
- Self-induced equality (both stamps backdated from the same review) is suppression-valid `>=`, never a withdrawal (`0801fc4`).

## Operational gotchas (cost real time this session)

- `poll-copilot-review.sh` exit 1 ≠ no review — check the API for post-ask reviews (bit us twice).
- Codex **auto-reviews every push** to this PR (rounds 8–10 arrived unrequested on interim pushes). Push-then-fix-more means new rounds appear mid-cycle; fetch reviews/threads fresh before every close-out.
- `render-reply-bodies.sh` requires `duplicate_of` to be a permalink URL string, not a numeric comment id.
- Local `mv` is aliased interactive — use `command mv -f` in scripted jq-rewrite loops.
- zsh doesn't word-split unquoted vars — `eval` the `record_provenance.py` `--commit` arg list.
- Provenance sidecar is head-keyed: re-record after EVERY push (`git rev-list FETCH_HEAD..HEAD` from a fresh `git fetch origin main`).
- Fixer dispatch pattern that worked: one opus `pr-comment-fixer-team` subagent per finding (or per family with one commit per finding), serial, report JSON at `/tmp/pr-comment-fixer-<id>.json`, audit exactly-one-commit + SHA before the next dispatch.
