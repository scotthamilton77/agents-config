# Fixture Delegation Addendum

Synthetic plugin rule whose filename (`delegation.md`) deliberately collides
with the shared rule of the same name, exercising append-merge parity between
the bash and Python installers (tests/ only).
